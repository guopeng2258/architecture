server:
  port: 8080

spring:
  datasource:
    url:
      jdbc:mysql://localhost:3306/mall_tiny?useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&transformedBitIsBoolean=true&allowMultiQueries=true&useSSL=false&allowPublicKeyRetrieval=true&tinyInt1isBit=false&allowLoadLocalInfile=true&allowLocalInfile=true&allowUrlInLocalInfile=true&allowPublicKeyRetrieval=true&allowPublicKeyRetrieval=true&allowPublicKeyRetrieval=true&allowPublicKeyRetrieval=true&allowPublicKeyRet
    username: root
    password: Tmt@123456
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  application:
    name:
      arch-mbg

mybatis:
  mapper-locations:
    - classpath:dao/*.xml
    - classpath:com/**/mapper/*.xml
  configuration:
    # ????????
    map-underscore-to-camel-case: true

logging:
  level:
    root: info
    com.perry.archmbg: debug

# 解决Springfox与Spring Boot 2.6+兼容性问题
springfox:
  documentation:
    auto-startup: false